{
  "name": "atoll-space",
  "version": "0.1.0",
  "description": "the floating archipelago — a spatial social web",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "drift": "node src/utils/drift.js"
  },
  "dependencies": {
    "firebase": "^10.8.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
